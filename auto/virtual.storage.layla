make(virtual_storage, type="virtual.storage", share='/ses.samba')

add(virtual_storage : file.proxy.web.server ? server_method)

show(virtual_storage)

delete(virtual_storage : server_method)

get(virtual_storage : server_method)

// virtual_storage
// 가상 디스크로, 실제로 메모리에는 할당되지만, 할당된 주소가 없어 접근이 특이함.
// get 메서드와 데이터 이름(정보명으로 가져올 수 있음)

<method="한글">

생성(가상_디스크, 타입="가상_스토리지", 공유='/ses.samba')

입력(가상_디스크 : 파일_프록시_웹_서버 ? 함수명)

보기(가상_디스크)

삭제(가상 디스크 : 함수명)

가져와(가상 디스크 : 함수명 )